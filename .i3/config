# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
font pango:Source Code Pro 9
#font pango : WenQuanYi Micro Hei Mono 10
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec --no-startup-id xterm 
bindsym $mod+Return exec --no-startup-id urxvt
#bindsym XF86WebCam --no-startup-id "xset dpms force off"
# kill focused window
bindsym $mod+Shift+q kill
# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run -fn 'Source Code Pro-8'
#bindsym $mod+d exec --no-startup-id dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
#bindsym $mod+d exec --no-startup-id --no-startup-id i3-dmenu-desktop
bindsym XF86ScreenSaver exec --no-startup-id "xset dpms force off && i3lock -i ~/.i3/gentoo.png"
# take screenshot
# YYYY-MM-DD-hh-mm-ss, no same file name
bindsym Print exec --no-startup-id scrot '%Y-%m-%d-%H.%M.%S.png' -e 'mv $f ~/Pictures/Scrot/'
#bindsym Print exec --no-startup-id scrot -s
# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+i focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+i move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent


# focus the child container
#bindsym $mod+d focus child
# Custom Variables For Workspaces
set $ws1 main
set $ws2 code
set $ws3 document
set $ws4 www
set $ws5 media
set $ws6 math
set $ws7 download
set $ws8 game
set $ws9 irc
set $ws10 music

# switch to workspace
bindsym $mod+1 workspace 1 $ws1
bindsym $mod+2 workspace 2 $ws2
bindsym $mod+3 workspace 3 $ws3
bindsym $mod+4 workspace 4 $ws4
bindsym $mod+5 workspace 5 $ws5
bindsym $mod+6 workspace 6 $ws6
bindsym $mod+7 workspace 7 $ws7
bindsym $mod+8 workspace 8 $ws8
bindsym $mod+9 workspace 9 $ws9
bindsym $mod+0 workspace 10 $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1 $ws1
bindsym $mod+Shift+2 move container to workspace 2 $ws2
bindsym $mod+Shift+3 move container to workspace 3 $ws3
bindsym $mod+Shift+4 move container to workspace 4 $ws4
bindsym $mod+Shift+5 move container to workspace 5 $ws5
bindsym $mod+Shift+6 move container to workspace 6 $ws6
bindsym $mod+Shift+7 move container to workspace 7 $ws7
bindsym $mod+Shift+8 move container to workspace 8 $ws8
bindsym $mod+Shift+9 move container to workspace 9 $ws9
bindsym $mod+Shift+0 move container to workspace 10 $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec --no-startup-id i3-msg exit

bindsym $mod+Shift+m exec --no-startup-id xcalib -invert -alter	
# resize window (you can also use the mouse for that)
mode "resize" {
# These bindings trigger as soon as you enter the resize mode

# Pressing left will shrink the window’s width.
# Pressing right will grow the window’s width.
# Pressing up will shrink the window’s height.
# Pressing down will grow the window’s height.
	bindsym j resize shrink width 10 px or 10 ppt
		bindsym k resize grow height 10 px or 10 ppt
		bindsym i resize shrink height 10 px or 10 ppt
		bindsym l resize grow width 10 px or 10 ppt

# same bindings, but for the arrow keys
		bindsym Left resize shrink width 10 px or 10 ppt
		bindsym Down resize grow height 10 px or 10 ppt
		bindsym Up resize shrink height 10 px or 10 ppt
		bindsym Right resize grow width 10 px or 10 ppt

# back to normal: Enter or Escape
		bindsym Return mode "default"
		bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {	
	mode dock
#modifier Mod1

		workspace_buttons yes
		status_command i3status --config ~/.i3/i3status.conf
#status_command $HOME/.i3/conky-i3bar
	#	colors {
			#background #FFFFFF
	#			statusline #999999
	##			focused_workspace #FFFFFF #FFFFFF #000000
	#			inactive_workspace  #FFFFFF #FFFFFF #999999
	#			urgent_workspace #b492b6 #b492b6 #cccccc
	#	}
}
####MUSIC BINDS####
# Volume
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -c 0 -q set Master 2dB+ unmute
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -c 0 -q set Master 2dB- unmute
bindsym XF86AudioMute exec --no-startup-id amixer -q set Master toggle
##xclock
bindsym XF86Sleep exec --no-startup-id "i3lock -i  ~/.i3/gentoo.png && systemctl suspend"
##xmms2
#bindsym XF86AudioPlay exec --no-startup-id xmms2 play
#bindsym XF86AudioStop exec --no-startup-id xmms2 stop
#bindsym Pause exec --no-startup-id xmms2 pause
#bindsym XF86AudioNext exec --no-startup-id xmms2 next
#bindsym XF86AudioPrev exec --no-startup-id xmms2 prev
#mocp
bindsym XF86AudioPlay exec --no-startup-id mocp -p
bindsym XF86AudioStop exec --no-startup-id mocp -s
bindsym Pause exec --no-startup-id mocp -P
bindsym XF86AudioNext exec --no-startup-id mocp -f
bindsym XF86AudioPrev exec --no-startup-id mocp -r
bindsym XF86Display exec --no-startup-id mocp -U
bindsym XF86WWW	      exec --no-startup-id chromium

###PROGRAM WS ASSIGN###
for_window [class="URxvt"] border none
for_window [class="Pcmanfm"] floating enable
for_window [class="File-roller"] floating enable
for_window [class="feh"] floating enable

assign [class="Emacs"] 2 code
for_window [class="Emacs"] border none fullscreen 
#for_window [class="NetBeans IDE 8.1"] floating enable
#3 document
assign [class="Zim"] 3 document
for_window [class="Zim"] border none
assign [class="Zathura"] 3 document
for_window [class="Zathura"] border none
assign [class="libreoffice"] 3 document
for_window [class="libreoffice"] floating enable
#4 www
assign [class="Firefox"] 4 www
for_window [class="Firefox"] border none 
assign [class="google-chrome"] 4 www
for_window [class="google-chrome"] border none 
#5 media
assign [class="mpv"] 5 media
for_window [class="mpv"] border none fullscreen
assign [class="Smplayer"] 5 media
for_window [class="Smplayer"] border none
assign [class="Qmmp"] 5 media
for_window [class="Qmmp"] floating enable
for_window [class="OSD Lyrics"] floating enable
assign [class="Audacious"] 5 media
for_window [class="Audacious"] floating enable
assign [class="Gimp"] 5 media
for_window [class="Gimp"] floating enable
#6 math
assign [class="Wxmaxima"] 6 math
for_window [class="Wxmaxima"] border none
assign [class="XMathematica"] 6 math
for_window [class="XMathematica"] border none
#5 download
assign [class="Amulegui"] 7 download
for_window [class="Amulegui"] border none
#8 game
assign [class="wesnoth"] 8 game
for_window [class="wesnoth"] border none fullscreen
assign [class="Steam"] 8 game
for_window [class="Steam"] border none
assign [class="CGoban"] 8 game
assign [class="KGS"] 8 game
for_window [class="CGoban"] floating enable
for_window [class="KGS"] floating enable
##autostart
exec --no-startup-id xrdb ~/.Xdefaults
exec --no-startup-id feh -z --bg-fill ~/wallpapers
exec --no-startup-id xgamma -gamma .7
exec --no-startup-id fcitx
exec --no-startup-id compton -b
#exec --no-startup-id compton --backbackend glx --paint-on-overlay --glx-no-stencil --vsync opengl --unredir-if-possible
#exec --no-startup-id compton --paint-on-overlay --glx-no-stencil --unredir-if-possible
#exec --no-startup-id mocp -S
exec --no-startup-id xset -dpms s off
#exec --no-startup-id xset dpms 1800

exec --no-startup-id aria2c -D
exec --no-startup-id emacs --daemon
#exec --no-startup-id sleep 10; xmodmap ~/.xmodmap
#exec --no-startup-id ss-local -c /etc/shadowsocks-libev/shadowsocks.json 

#exec --no-startup-id setxkbmap -option ctrl:swapcaps
#exec --no-startup-id setxkbmap -option ctrl:nocaps 
